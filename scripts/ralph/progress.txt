# Ralph Progress Log
Started: Mon Apr 13 18:18:43 UTC 2026

## Codebase Patterns
- Event payloads use `dict[str, str | int | float | bool | None]` type — see `LithosEvent.payload`
- File-watcher delete handler emits NOTE_DELETED before calling knowledge.delete() to preserve doc_id access
- enrich_queue schema uses `attempts` column for retry tracking; drain methods filter by `attempts < max_enrich_attempts`
- Pre-existing typecheck errors in server.py conflict_resolve (lines ~1737-1739) — not introduced by this work
- Pre-existing format issue in tests/test_conflict_resolve.py — not introduced by this work
- StatsStore schema tests use `PRAGMA table_info(<table>)` to check columns as a set

---

## 2026-04-13 - US-001
- What was implemented:
  - Added knowledge_id to FINDING_POSTED event payload in lithos_finding_post (when present)
  - Moved NOTE_DELETED emission before knowledge.delete() in file-watcher and added doc id to payload
  - Added `attempts INTEGER NOT NULL DEFAULT 0` column to enrich_queue schema
  - Added `max_enrich_attempts: int = 3` to LcmaConfig
  - Updated test_enrich_queue_columns to expect "attempts" in column set
- Files changed:
  - src/lithos/server.py (FINDING_POSTED payload, NOTE_DELETED payload + ordering)
  - src/lithos/lcma/stats.py (enrich_queue schema)
  - src/lithos/config.py (LcmaConfig.max_enrich_attempts)
  - tests/test_stats.py (test_enrich_queue_columns)
- **Learnings for future iterations:**
  - File-watcher delete handler must emit event BEFORE knowledge.delete() because delete evicts _meta_cache
  - The lithos_note_delete MCP tool already included "id" in NOTE_DELETED payload, only file-watcher was missing it
  - LcmaConfig has model validators for rerank_weights and note_type_priors — add new simple fields before them
  - 915 unit tests pass; 309 integration tests deselected with `-m "not integration"`
---
